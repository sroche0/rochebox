// Abort immediately if a command fails, so a broken/incomplete update doesn't
// silently fall through to launching the server on stale files.
@ShutdownOnFailedCommand 1
@NoPromptForPassword 1
force_install_dir __STEAMAPPDIR__
login anonymous
app_update __STEAMAPPID__ __BETA_ARGS__validate
quit